<h1>Pets#index</h1>
<p>Find me in app/views/pets/index.html.erb</p>

<!-- div.container>div.row>div.col-6.offset-3 -->
<div class="container">
    <% @pets.each do |pet| %>
  <div class="row">
    <div class="col-6 offset-3">
      <div class="card">
    <h2><%= pet.name %></h2>
    <p><%= pet.address %></p>
    <p><%= pet.species %></p>
    <p><%= pet.found_on %></p>
    <% end %>
    </div>
    </div>
  </div>
</div>

